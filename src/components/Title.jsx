import React from "react";

const Title = ({ title }) => {
    return (
        <>
            <h1 className="display-2 mt-2 text-center fw-bold">{title}</h1>
        </>
    );
}

export default Title;